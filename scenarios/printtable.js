const table=(num)=>{
    for(let i=1;i<=10;i++){
        var result=i*num
        console.log(`${num} x ${i} = ${result}`)
    }
}
//table(2)
//table(3)
//table(4)
table(5)


const tableprint=(number)=>{
    for(let i=1;i<=10;i++){
        var result=i*number
        console.log(`${number} x ${i}= ${result}`)
    }
}
tableprint(6)